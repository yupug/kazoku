<%= form_for(@kazoku) do |f| %>
  <% if @kazoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kazoku.errors.count, "error") %> prohibited this kazoku from being saved:</h2>

      <ul>
      <% @kazoku.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
